import React, { useEffect, useState } from "react";
import {
  StyleSheet,
  Image,
  Dimensions,
  TouchableOpacity,
  ScrollView,
  Text,
  View,
  SafeAreaView,
  ImageBackground,
  FlatList,
} from "react-native";

export const Settings = () => {
  return (
    <SafeAreaView>
      <View>
        <Text>sdfasdf</Text>
      </View>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({
})